syntax = "proto2";

package tensorflow.lingvo;

import "tensorflow/core/framework/tensor.proto";

// A message Record is just a few named tensors.
message Record {
  map<string, tensorflow.TensorProto> fields = 1;
}
